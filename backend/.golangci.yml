version: "2"

run:
  timeout: 5m
  build-tags:
    - integration

linters:
  enable:
    - govet
    - staticcheck
    - unused
    - ineffassign
    - errcheck

formatters:
  enable:
    - gofmt
    - goimports
